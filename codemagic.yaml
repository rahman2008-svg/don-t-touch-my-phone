workflows:
  android-release:
    name: Android Release

    environment:
      java: 17

      android_signing:
        - your_keystore

    scripts:
      - chmod +x gradlew
      - ./gradlew assembleRelease

    artifacts:
      - app/build/outputs/apk/release/*.apk
